# ===================================
#  List Behavior Config
# ===================================

# Model List Column configuration
list: $/hao/dictionary/models/vocabulary/columns.yaml

# Model Class name
modelClass: Hao\Dictionary\Models\Vocabulary

# List Title
title: 'hao.dictionary::lang.permission.label.manage_vocabulary'

# Link URL for each record
recordUrl: hao/dictionary/vocabularies/update/:id

# Message to display if the list is empty
noRecordsMessage: hao.dictionary::lang.list.no_records.vocabulary

# Records to display per page
recordsPerPage: 20

# Display page numbers with pagination, disable to improve performance
showPageNumbers: true

# Displays the list column set up button
showSetup: true

# Displays the sorting link on each column
showSorting: true

# Default sorting column
# defaultSort:
#     column: created_at
#     direction: desc

# Display checkboxes next to each record
showCheckboxes: true

# Toolbar widget configuration
toolbar:
    # Partial for toolbar buttons
    buttons: list_toolbar

    # Search widget configuration
    search:
        prompt: backend::lang.list.search_prompt

# Config filters
filter: config_filter.yaml
